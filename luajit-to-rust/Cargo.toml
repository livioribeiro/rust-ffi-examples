[package]
name = "luajit-to-rust"
version = "0.1.0"
# Modified based on python version
authors = ["Livio Ribeiro <livioribeiro@outlook.com>"]

[lib]
name = "ffi_example"
crate-type = ["dylib"]

[dependencies]
libc = "*"
